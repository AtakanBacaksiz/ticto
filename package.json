{
  "name": "ticto",
  "version": "1.0.0",
  "description": "PeerJS signalling server for ticto",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "npx jest"
  },
  "dependencies": {
    "peer": "^0.9.1"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
